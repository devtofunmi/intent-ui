# Intent UI Environment Configuration

# 1. Tambo AI SDK (Required for component generation)
# Get your keys at https://tambo.ai
VITE_TAMBO_PUBLIC_KEY=your_public_key

# 2. GitHub Integration (Required for production OAuth)
# Create an OAuth App at https://github.com/settings/developers
# Redirect URI should be: https://your-domain.com/auth/github/callback
VITE_GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret

# 3. Vercel Integration
# Deployment token is usually handled in-app via user input, 
# but you can define it here if needed for server-side logic.
# VERCEL_TOKEN=your_access_token